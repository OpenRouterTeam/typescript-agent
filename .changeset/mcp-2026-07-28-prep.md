---
'@openrouter/mcp': patch
---

Correct the self-reported client version sent to MCP servers (`0.1.0` → `0.0.1`, matching
the package), cover the Streamable HTTP → SSE fallback with tests, and document the
negotiated protocol revision.

Marks `sessionId`, `onElicitation`, and `transport: 'sse'` as `@deprecated`: MCP revision
`2026-07-28` removes protocol-level sessions and server-initiated elicitation and
deprecates HTTP+SSE. No runtime behavior changes — the pinned SDK still negotiates
`2025-11-25`, and no released SDK speaks `2026-07-28` by default yet.
